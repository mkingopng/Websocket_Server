# server-protocols/src

Contains the source code for the `server-protocols` crate, which defines the WebSocket protocol messages and data structures for OpenLifter.

- `lib.rs`: Main library file with message enums and data types for client-server communication. 