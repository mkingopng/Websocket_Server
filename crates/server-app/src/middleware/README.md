# middleware/

Contains middleware components for the backend server, such as rate limiting and request pre-processing.

- `rate_limit.rs`: Implements rate limiting middleware
- `middleware_tests.rs`: Unit tests for middleware components
- `mod.rs`: Module declarations 